name: Deploy MLEP Site

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python 3.11
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
    
    - name: Create data directory
      run: |
        mkdir -p data
        
    - name: Initialize database
      run: |
        python -c "
        from app.db import engine, Base
        from app.models.user import User
        Base.metadata.create_all(bind=engine)
        print('Database initialized successfully')
        "
    
    - name: Run tests
      run: |
        python -m pytest tests/ -v || echo "No tests found, skipping..."
    
    - name: Build static assets (if needed)
      run: |
        # Add any build steps for static assets here
        echo "Static assets ready"
    
    # For deployment to a cloud service, add deployment steps here
    # Example for Railway, Heroku, or other platforms
    
    - name: Deploy to Railway (example)
      if: github.ref == 'refs/heads/main'
      run: |
        echo "Add Railway deployment commands here"
        # railway login --token ${{ secrets.RAILWAY_TOKEN }}
        # railway up
